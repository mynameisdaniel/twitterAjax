
<!-- <% if current_user.follows?(user) %>
  <%= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->

<button data-follow-state="<%= current_user.follows?(user) ? 'followed' : 'unfollowed' %>"
        data-user-id="<%= user.id %>" class="follow-toggle">

</button>

<script>
  $(function () {
    $("button.follow-toggle").followToggle();
  });
</script>